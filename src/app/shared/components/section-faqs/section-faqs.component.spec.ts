import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';

import { SectionFaqsComponent } from './section-faqs.component';

describe('SectionFaqsComponent', () => {
  let component: SectionFaqsComponent;
  let fixture: ComponentFixture<SectionFaqsComponent>;



  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
